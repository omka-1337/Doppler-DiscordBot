discord.py>=2.3.0
fastapi>=0.141.1
uvicorn>=0.52.1
# uvicorn needs this to accept WebSocket upgrades, and the dashboard's live
# log tail is a WebSocket. It arrived transitively until the provider SDKs
# moved to the broker, so it is declared here rather than depended on.
websockets>=13.0
jinja2>=3.1.6
httpx>=0.28.1
pydantic>=2.13.4
python-dotenv>=1.2.2
aiosqlite>=0.22.1
python-multipart>=0.0.32
wavelink>=3.4.0
psutil>=6.1.0
itsdangerous>=2.2.0
markdown>=3.7
deep-translator>=1.11.4
